import React from 'react';
import Events from '../Events';

const UpcomingWebinars = () => {
  // We'll reuse the Events component but filter for upcoming webinars
  return <Events />;
};

export default UpcomingWebinars;