import React from 'react';
import Events from '../Events';

const PastEvents = () => {
  // We'll reuse the Events component but with past events data
  return <Events />;
};

export default PastEvents;