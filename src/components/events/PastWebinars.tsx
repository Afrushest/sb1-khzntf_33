import React from 'react';
import Events from '../Events';

const PastWebinars = () => {
  // We'll reuse the Events component but filter for past webinars
  return <Events />;
};

export default PastWebinars;